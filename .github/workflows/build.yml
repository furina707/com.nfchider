name: Build Xposed Module

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Decode Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > ${{ github.workspace }}/nfchider.jks

      - name: Build Release APK
        run: |
          gradle assembleRelease --no-daemon
        env:
          KEYSTORE_PATH: ${{ github.workspace }}/nfchider.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: Sign APK (if needed)
        run: |
          mv app/build/outputs/apk/release/*.apk NfcHider-v1.0-release.apk || true

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NfcHider-v1.0-release
          path: NfcHider-v1.0-release.apk
